version: 1
frontend:
  phases:
    preBuild:
      commands:
        - "export NODE_OPTIONS=--no-deprecation"
        - "nvm install 22"
        - "nvm use 22"
        - "node -v"
        - "yarn -v"
        - "yarn install"
    build:
      commands:
        - "export NODE_OPTIONS=--no-deprecation"
        - "yarn build"
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - .next/cache/**/*
      - .yarn/cache/**/*
      - node_modules/**/*
